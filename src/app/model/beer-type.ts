import {stringify} from "@angular/compiler/src/util";
import {compareNumbers} from "@angular/compiler-cli/src/diagnostics/typescript_version";

export class BeerType {
  name: string;
  water: number;
  sugar: number;
  yeast: number;
  hop: number;
}


// constructor(name: string, water: number, sugar: number, yeast: number, hop: number) {
//   this.name = name;
//   this.water = number;
//   this.sugar = number;
//   this.yeast = number;
//   this.hop = number;
//
// }
